extends layout

block content 
 h2 Details
 ul 
  li 
    img(alt='' src=`/${part.img}`)
  li Name: #{part.name}
  li description: #{part.description}
  li category: #{part.category.name}
  li price: $ #{part.price}
  li quantity: #{part.quantity}
 hr
 ul 
  li
    a(href=`${part.url}/update`) Update Part
  li 
    a(href=`${part.url}/delete`) Delete Part